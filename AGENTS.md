# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Impala") is a greenfield project for a parcel delivery application ("application de livraison des colis"). As of now, the repository contains only a `README.md` with no source code, no dependencies, and no build/test/lint configuration.

- **No services to run**: There are no backend, frontend, or database services configured yet.
- **No dependencies**: No `package.json`, `requirements.txt`, `go.mod`, or other dependency manifests exist.
- **No build/test/lint tooling**: No test frameworks, linters, or build tools are configured.

Once the technology stack is chosen and code is added, this file should be updated with:
- How to install dependencies
- How to run services (dev mode)
- How to run tests and lint
- Any non-obvious caveats discovered during development
